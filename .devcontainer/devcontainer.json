{
    "name": "Python and Jupyter",
    "build": {
        "dockerfile": "Dockerfile",
        "args": { "VARIANT": "3" }
    },
    "postCreateCommand": "pip install -r requirements.txt",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter"
            ]
        }
    }
} 